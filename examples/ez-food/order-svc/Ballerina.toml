[package]
org = "example"
name = "ordersvc"
version = "0.1.0"

[build-options]
observabilityIncluded = true

[persist]
datastore = "inmemory"
module = "ordersvc.store"

[[platform.java21.dependency]]
groupId = "io.ballerina.stdlib"
artifactId = "persist.inmemory-native"
version = "1.2.0"

